# 钟日 v2 AI 开发规则

## 修改原则

- 修改前说明影响范围。
- 不跨架构层调用。
- 不随意新增依赖。
- 不改变已有 Schema 语义。
- 大改动拆分提交。

## 代码规则

- 使用 TypeScript strict。
- 优先保持简单。
- 不重复造轮子。
- 不生成无用抽象。
- 不为了未来需求提前复杂化。

## 测试规则

修改后必须运行：

```bash
npm run build
npm run test
```
